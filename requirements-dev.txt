# Development / CI tooling. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# Versions are lower-bounded on known-good releases for reproducible dev setups.
flake8>=7.0.0
pytest-cov>=5.0.0
pre-commit>=3.7.0
codespell>=2.2.0  # spell checking for source and docs
